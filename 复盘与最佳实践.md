# SteelDigitize Pro — 技术复盘与最佳实践

> 记录：2026-08-09 · 覆盖识别引擎切换、桌面化、自动更新全链路

---

## 一、GitHub 相关功能详解

### 1. 用到的 GitHub 功能

| 功能 | 作用 | 本项目用法 |
|---|---|---|
| Git 仓库 | 版本控制、多人协作、历史回溯 | 全量代码，分支 main |
| GitHub Actions | 云端 CI/CD，免费提供 Windows/macOS/Linux 构建机 | 推送 `v*` 标签后自动打包并发布 |
| GitHub Releases | 发布页 + 文件下载（可公开、免登录） | 存放 exe/dmg 安装包 + 更新元数据 |
| GitHub API | 程序化查询/上传/删除 | 查询最新 Release、上传资产、验证 |
| Personal Access Token | 认证（推代码/调 API） | 发布与验证（classic：`repo`+`workflow`） |

### 2. 为什么 GitHub 能当"桌面应用发布中心"

桌面应用（Electron）的发布 = 两件事：
1. **把安装包放到一个别人能下载的地方** → GitHub Releases 免费提供（公开仓库免登录）
2. **提供"哪个是最新版"的元数据** → electron-builder 发布时自动生成 `latest.yml`（Windows）/ `latest-mac.yml`（Mac），记录版本号、文件名、校验和

应用内置的 electron-updater 检查更新流程：

```
应用启动/点检查更新
  → 请求更新源（GitHub Releases 或镜像）里的 latest.yml
  → 解析出版本号 → 与本地版本 semver 比较
  → 有新版 → 下载安装包（支持断点/校验）→ 重启安装
```

关键点：**electron-builder 负责打包 + 生成更新元数据 + 上传 Release，electron-updater 负责客户端检查/下载/安装**，GitHub 只当"免费的存储和版本查询中心"。

### 3. 别的方式可以吗

可以，electron-builder/electron-updater 原生支持多种发布源：

| 方案 | 优点 | 缺点 |
|---|---|---|
| GitHub Releases | 免费、GitHub Actions 闭环、公开免登录 | 国内访问不稳定 |
| GitLab Releases | 自托管可控 | 需维护服务器/CI |
| S3 / 阿里云 OSS / 七牛 | 国内快、稳定 | 要花钱、要密钥 |
| Generic HTTP（自建/CDN/镜像） | 任意静态服务器即可 | 需自己维护文件与元数据 |
| 网盘（蓝奏/百度） | 最方便手动分发 | 无 API，不能做自动更新 |

结论：**手动分发用网盘/微信，自动更新用 GitHub/S3/自建 HTTP**。我们选了 GitHub 做发布源 + 国内镜像做下载源。

---

## 二、问题复盘（按时间线）

### 1. 识别通道选错 → A0102
- **现象**：REST 调用 `scan-business.quark.cn/vision` 报 `A0102 account not found or invalid`
- **根因**：转 Excel（image-to-excel）是夸克 **Agent 专用能力**，官方文档明确不支持 API 接入；REST 通道 + API Key 根本不匹配
- **解决**：改用官方 yescan SDK（Agent 通道，`X-Appbuilder-From=cli`），并统一在 `quark.py` 封装
- **教训**：先查官方文档的能力/通道限制，不要凭字段猜

### 2. 并发触发限流 → A0300
- **现象**：3 张并发识别报 `QPS limit exceeded`
- **解决**：`SCAN_MAX_CONCURRENCY=2` + A0300 自动退避重试（1.5s×n，最多 3 次）

### 3. 文件名同名覆盖
- **现象**：多张同秒上传只落盘 1 张图，审核区单据串图
- **解决**：文件名加 uuid 后缀

### 4. 识别后规格拆分丢失
- **现象**："密闭套管150×29"整体进了名称栏
- **根因**：识别后只跑了结构化解构，没跑完整的纯代码校准
- **解决**：`_recognize_one` 调用 `calibrate_items`（结构→规格拆分→品名对齐→单位补全）

### 5. 纯规格行漏检
- **现象**："6,5米×12支"被当成品名
- **根因**：`is_pure_spec` 只认纯数字符号，不认"米/支"等单位字
- **解决**：新增 `is_spec_like`（数字+单位字组合判定），继承上方品名

### 6. 桌面端端口残留
- **现象**：新实例连到旧后端，报"前端未构建"
- **解决**：单实例锁 + 退出必杀后端 + 启动前健康探测复用

### 7. 自动更新链路（最曲折的一段）

| 问题 | 根因 | 解决 |
|---|---|---|
| Unable to find latest version | electron-builder **默认创建 draft（草稿）Release**，草稿对更新不可见 | publish 配置加 `releaseType: "release"` |
| 版本号错乱 | work 副本同步覆盖了 package.json 版本（1.0.2→1.0.0） | 版本号单一来源，发布前校验 tag 与版本一致 |
| Release 资产 untagged | 删远程 tag 导致 Release 与标签失联 | 发布流程不再删 tag；重建走正式流程 |
| latest.yml 解析失败（YAMLException） | 手动 `-F` 上传被 GitHub 存成 multipart 包装 | 改用 `--data-binary` 纯内容上传；日常应让 electron-builder 自动上传 |
| ERR_CONNECTION_CLOSED | 国内访问 github.com 被墙（api 通、主站不通） | 更新源切到加速镜像（gh-proxy），并保留手动下载兜底 |
| Mac 自动更新不可用 | 未签名（Apple Developer 证书 + notarization） | 短期手动装 dmg；长期补签名 |
| Mac dmg 需本地补 | workflow 只构建 Windows | 应改为多平台矩阵（windows+macos） |

---

## 三、最佳实践（来自社区与官方文档）

### 1. electron-builder 发布配置
- **Release 类型必须显式**：`publish.releaseType = "release"`（默认 draft，官方文档/源码确认：`PublishOptions.draft=false → release`，默认 draft）
- 优先让 **electron-builder 自己发布**（`--publish always` + GitHub provider），避免手动上传的 multipart/文件名坑
- 资产命名避免空格（GitHub 会自动把空格转成点，导致元数据文件名对不上）

### 2. GitHub Actions 构建矩阵
官方/社区最佳实践是 **多平台矩阵并行构建**：
```yaml
matrix:
  os: [windows-latest, macos-latest]
```
每个平台打自己的安装包 + `latest*.yml`，统一发布到同一 Release。tag 触发。

### 3. macOS 自动更新必须签名
electron-builder 官方文档明确：**macOS 应用必须签名（+ notarization）自动更新才能工作**。未签名的 Mac 应用只能手动装 dmg。Windows 未签名可更新（仅 SmartScreen 提示）。

### 4. 国内更新源
- 官方建议国内项目配置镜像（Electron 下载镜像、更新源镜像）
- 我们的方案：GitHub Releases 发布 + 客户端 `setFeedURL` 指向 `gh-proxy.com` 加速镜像
- 兜底：更新失败时前端提示手动下载链接

### 5. 版本管理
- 版本号只改 `electron/package.json` 一处
- `git tag vX.Y.Z` 必须与版本号一致（可在 workflow 里校验，防止错版）
- 发布流程固定：改版本 → push → tag → 云端自动打包发布

### 6. 更新体验细节
- 自动更新下载完再提示"重启安装"（autoDownload=false，用户确认）
- 支持下载进度展示（download-progress 事件）
- 更新失败要能重试 + 手动下载兜底

---

## 四、待办改进

- [ ] workflow 增加 macos-latest（Mac dmg 云端自动出，不再本地补）
- [ ] Mac 自动更新：补 `.zip` target + Apple 开发者签名/notarization（长期）
- [ ] 发布前校验 tag 与 package.json 版本一致（workflow step）
- [ ] 沉淀一键发布脚本 `release.sh`（改版本→push→tag→等云端）
