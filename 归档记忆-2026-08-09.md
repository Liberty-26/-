# 数字化工作台（SteelDigitize）— 项目归档记忆

> 归档日期：2026-08-09 · 目的：跨会话延续项目记忆，新会话先读本文件再动手

---

## 一、项目快照（当前状态）

- **产品名**：数字化工作台（旧名 SteelDigitize Pro，界面已全部改名为"数字化工作台"）
- **当前版本**：v1.0.10（GitHub Release 正式发布，Windows exe + macOS dmg/zip 共 8 个资产齐全）
- **仓库**：`github.com/Liberty-26/-`（公开仓库，main 分支）
- **本地源码**：`/Users/libaodian/Desktop/工作区/SteelDigitize-github/`
- **工作区状态**：干净（无未提交改动；UI 反复设计的所有未提交改动已于 2026-08-09 全部还原）
- **核心定位**：建材五金经营者（单人、非技术背景）的本地工作台：拍送货单 → 自动识别成表格 → 人工对照原图审核 → 确认入库 → 工作助手整理进 Excel 对账单。不公开分发，私下传 / 服务器部署。

### 技术栈与目录

- 后端：FastAPI + SQLite（`backend/`），识别走夸克扫描王 Agent 通道（yescan SDK）
- 前端：React 19 + Vite + 自定义 CSS（`frontend/`），图标库 lucide-react
- 桌面壳：Electron（`electron/`），内置 PyInstaller 打包的后端，electron-updater 自动更新
- 云端：GitHub Actions 双平台并行构建 + 自动发布（`.github/workflows/build-desktop.yml`）
- 数据目录（桌面版）：用户数据目录 `userData/appdata/`（数据库 data.db、上传 uploads、配置 .env 都在这里，更新不丢）

---

## 二、关键架构决策（改代码前必读）

### 1. 识别链路（已定型，勿改回）
- 最终方案：**扫描王 image-to-excel → xlsx 内存解析 → 结构化 JSON → 纯代码校准**（不落盘、不直接出交付表格）
- 转 Excel 是夸克 **Agent 专用能力**：必须用 yescan CLI/SDK（`X-Appbuilder-From=cli`），REST 通道会报 A0102
- 调用统一封装在 `backend/quark.py`；批量并发默认 2（并发 3 会触发 A0300 限流），A0300 自动退避重试
- 金额**不识别**：只识别数量/单价，每行金额与合计由代码计算
- 校准：纯代码（`calibrate.py`）——结构问题交给代码、语义问题交给品名库、规则问题代码兜底；**DeepSeek 语义校准已删除**
- 单号/日期自动提取并保留，人工审核可改、不可丢

### 2. 待审核队列（实时同步）
- 全局共享数据源：`frontend/src/contexts/QueueContext.tsx`
- 首页识别保存成功 → `addPending` 立即入队；审核区确认入库 → `removePending`
- 首页单据流与审核区左侧队列读同一份数据，**无需刷新**
- 侧边栏"待审核"角标 = queue.length（Layout 直接渲染）

### 3. 桌面化与更新
- Electron 单实例锁 + 退出必杀后端 + 启动健康探测
- **版本指纹机制**：`/api/health` 返回 `pid + dist_mtime`，Electron 启动时对比自身 `resources/frontend/dist/index.html` 时间戳；不一致 = 旧版本残留后端 → 杀掉重启。**这是"更新了但界面没变"的关键修复，勿删**
- 配置（API key 等）存 `.env`：桌面版经 `CONFIG_DIR` 环境变量指向用户数据目录（升级不丢）；开发模式在 `backend/.env`
- 品名种子：`品名种子清单.csv` 打包进应用资源（extraResources），启动时经 `MATERIALS_SEED_CSV` 环境变量导入（幂等）；桌面版数据库为空时靠它填充
- 更新源：GitHub Releases 发布 + 客户端 `setFeedURL` 指向 gh-proxy 加速镜像；Mac 未签名只能手动装 dmg（自动更新会报签名校验错误，属预期）

### 4. 发布流程（已自动化）
- 发版 = 改 `electron/package.json` 版本号 → push main → `git tag vX.Y.Z && git push origin vX.Y.Z`
- GitHub Actions 并行构建 Windows（exe）+ macOS（dmg+zip），`--publish always` 自动发布正式 Release（非草稿）
- 验证：Release 8 资产齐全 + 镜像 `latest.yml`/`latest-mac.yml` 为纯 YAML 且版本/文件名匹配
- 构建约 4-5 分钟（并行矩阵 + pip/npm 缓存）

---

## 三、踩坑清单（都是真金白银换来的）

| 问题 | 根因 | 解法 |
|---|---|---|
| A0102 账户无效 | 用 REST 调 Agent 专用能力 | 统一走 yescan CLI/SDK |
| A0300 限流 | 并发 3 超 QPS | 并发 2 + 自动退避重试 |
| 审核区与首页不同步 | 各自维护列表 | 共享 QueueContext |
| 更新后界面没变 | 复用了旧版本残留后端 | health 返回版本指纹，不一致杀掉重启 |
| 每次更新 API key 丢失 | .env 写在应用安装目录，更新被覆盖 | CONFIG_DIR 指向用户数据目录 |
| 品名库为空 | 种子 CSV 未打包进应用 | extraResources + 环境变量导入 |
| 找不到最新版本 | electron-builder 默认发草稿 Release | publish.releaseType="release" |
| latest-mac.yml 解析失败 | 手动上传变 multipart 包装 | --data-binary 纯内容上传；日常让 electron-builder 自动传 |
| Mac 自动更新失败 | 未签名（Squirrel 强制校验） | 短期手动装 dmg；长期 Apple 开发者签名+公证 |
| Mac 缺 zip 报错 | electron-updater 只认 zip | mac target 加 zip |
| 预览服务打不开 | vite 只监听 IPv6 localhost | `npm run dev -- --host 127.0.0.1` |
| 本机 sqlite 打不开 | 系统 Python sqlite 旧/沙箱限制 | 用 `backend/.venv/bin/python` 操作数据库 |

---

## 四、用户偏好与协作规则（非常重要）

1. **中文沟通，直接给结论**；不要长篇大论解释过程
2. **识别额度要钱**：未经用户批准，不要自行跑识别测试
3. **反对挤牙膏式修 bug**：要求全局扫描、一次性处理，不要"改一个问一个"
4. **UI 改动红线**：用户对反复"精修"极度反感；改 UI 必须给出**全局性、一眼可见的变化**，且**先预览确认再发版**（预览地址 127.0.0.1:5174 + 桌面截图）；方向未定前不要擅自动 UI
5. **版本号不得写死**：关于页/更新提示必须动态显示真实版本
6. **保留原版纯代码约束**（规格拆分、纯规格行、品名对齐、单位补全）——用户明确强调过多次，不许丢
7. 产品叫"数字化工作台"，界面/窗口标题已统一；安装包文件名维持 SteelDigitize-Pro（发布链路不变）
8. 桌面/下载文件夹要整洁：安装包不要放桌面，用完清理
9. 自定义 agent、私下传/服务器部署，不是大众分发产品

---

## 五、待办与未决

- [ ] **Mac 自动更新**：Apple Developer 签名 + notarization（$99/年）或"绕过 ShipIt"方案；当前保持手动装 dmg
- [ ] **UI 整体方向未定**：用户对浅色/深色多轮方案均不满意，最终全部回退；未来动 UI 前必须先给可预览的全局方案
- [ ] 开发库里残留 4 张测试单据（backend/data.db），是否迁移到桌面版库——待用户确认
- [ ] 发布前自动校验 tag 与版本一致（workflow step，可选）
- [ ] 一键发布脚本 release.sh（改版本→push→tag，可选）
- [ ] 定时功能（cron skill）、更多 agent 技能——用户提过，非本期

---

## 六、安全提醒

- 对话中曾明文出现 GitHub PAT（ghp_48T6... 等），**强烈建议用户吊销换新**
- `.env`、data.db 含敏感配置，**不要**打包进设计交付包或上传公开渠道
- 发版/上传资产需 GH_TOKEN：Actions 用 `secrets.GITHUB_TOKEN`，手动操作用用户提供的 PAT

---

## 七、常用命令速查

```bash
# 开发模式（Mac）
cd backend && .venv/bin/python -m uvicorn main:app --port 8000
cd frontend && npm run dev -- --host 127.0.0.1 --port 5174

# 构建验证
cd frontend && npm run build

# 发版
git add -A && git commit -m "vX.Y.Z：说明"
git push origin main
git tag vX.Y.Z && git push origin vX.Y.Z

# 操作数据库（系统 sqlite 打不开时）
backend/.venv/bin/python -c "import sqlite3; ..."
```
