# SteelDigitize Pro — 项目经验笔记

> 来源：项目从 MVP 到识别准确率深度优化的完整过程复盘
> 定位：通用规律，不是技术清单。每条 = 发生了什么 → 学到了什么 → 以后怎么做
> 日期：2026-08

---

## 一、AI 的能力边界：什么该交给模型，什么必须靠代码

### 1.1 结构问题永远不要押在模型身上

**发生了什么**：识别 prompt 反复要求千问"品名行不要输出、空 name 要继承"，模型偶尔遵守偶尔不遵守——品名行被输出成 qty=0 的幽灵行、空 name 行漏继承，不稳定。后来把"品名行归属"改成语义校准的规则，模型又和代码重复处理，反而混乱。

**学到了什么**：视觉模型的输出结构（行归属、空字段、字段错位）天然不稳定，再好的 prompt 也只是"概率上更好"。凡是**可以用确定性规则判定的事情**（这行是不是品名行？name 是不是纯规格？），交给代码就是 100%，交给模型就是 80-95%。

**以后怎么做**：识别链路分层——**结构问题（归属/继承/幽灵行/纯规格错位）→ 纯代码解构；语义问题（名称归一化、形近字、混写拆分）→ 模型；规则问题（数值范围、单位白名单）→ 代码兜底**。每一层只干自己擅长的事，绝不混层。

### 1.2 模型的"工具描述"会被严格遵守——包括错误的内容

**发生了什么**：Agent 把对账单写到了 C 盘桌面，而用户配置的是 G 盘。根因是 spreadsheet_create_new 的工具描述里写了"默认~/Desktop/对账单.xlsx"，模型读到后每次都填这个路径。

**学到了什么**：模型会**逐字遵守工具描述和系统提示**。你在描述里给了一个默认值，它就永远用这个值——它不会去查系统配置，也不会"觉得"应该用用户配置的路径。这是"提示词里不能写业务配置"的铁律。

**以后怎么做**：业务配置（路径、目录、账号）绝不硬编码进 prompt/工具描述。改为**代码层保险丝**：工具执行前强制把 filepath 覆盖为系统配置值（`if 配置存在: args["filepath"] = 配置`）。配置的优先级靠代码保证，不靠提示词祈求。System Prompt 只告诉模型"去哪里看"，执行层才是最后防线。

### 1.3 模型做语义纠错是强项，但要有"回滚护栏"

**发生了什么**：DeepSeek 校准把"V型卡"改成"v型卡"（无意义的大小写纠正），把"镀锌钢丝网"等正常品名乱改。后来加了护栏：**模型改品名前必须命中品名库**（标准名或别名前缀匹配），未命中就回滚原名并标 issue。

**学到了什么**：模型的语义纠错能力是真实的（排中管→排水管这种手写误识别它纠对了），但**没有约束的模型输出会过度修正**——它会把没问题的数据也"改一遍"来表现自己。AI 修正必须经过一道"权威来源校验"（品名库、白名单）才能落地。

**以后怎么做**：模型输出 → 代码校验（改了什么？新值是否在权威集合里？）→ 通过才接受，否则回滚 + 标 issue。AI 的建议是候选，代码的校验是裁决。

---

## 二、关于准确率的认知：从"觉得能99%"到认清现实

### 2.1 准确率的瓶颈不是模型，是数据形态

**发生了什么**：项目目标从"90% → 接近99%"，真实测试后发现：拖后腿的从来不是识别不出，而是**品名单行版式、名称规格混写、字段错位、幽灵行**这些"结构形态问题"。同一种模型，品名逐行展开的单据识别得很好，品名单独一行的单据就错。

**学到了什么**：手写单据识别的上限由**版式多样性**决定，不是由模型能力决定。每多一种版式，就要多一类专门处理。"换更强的模型"提升的是每个版式内的精度，结构问题要靠代码解构 + 版式示例。

**以后怎么做**：先盘点版式类型（品名单行、混写、纯规格错位、表头混入），每种版式对应一层确定性处理；用真实照片建基线，**每次改动必须有量化结果，不允许"感觉变好了"**。

### 2.2 测试前先检查数据——18张里有5张是重复的

**发生了什么**：基线测试跑了 10 分钟（18 张 × 2 次 API 调用），跑完发现前 5 张文件字节数完全一致，是同一张照片的重复副本，实际独立样本只有 13 张。

**学到了什么**：测试集的质量先于测试本身。重复样本会放大某些结果的权重，让"基线"失真。

**以后怎么做**：跑批量测试前先按文件哈希去重；报告里标注样本独立性。

### 2.3 准确率提升的最大杠杆：把"模型该干的"和"代码该干的"分开

**发生了什么**：v1 把结构问题交给模型（prompt 要求），准确率不稳；v2 把结构问题全部移到代码（structural_decompose），13 张独立照片**零空 name、零幽灵行**——结构问题从"概率"变成"必然"。

**学到了什么**：对确定性问题，代码是 100%，模型是概率。**最大杠杆不是换模型、不是堆 prompt，是把确定性问题从模型手里夺回来**。语义问题留给模型后，模型的负担小了，反而更准。

---

## 三、关于提示词的经验

### 3.1 提示词里要告诉模型"什么不可能"，而不只是"要什么"

**发生了什么**：prompt 前几版只给正向指令（"品名行不输出""每行填品名"），模型还是把 "4×400" 当品名。后来加了"品名不可能只包含数字和符号""规格可能是不含数字的简短描述"，再配合代码 is_pure_spec 兜底。

**学到了什么**：负向约束（"什么一定不是 X"）比正向指令更能帮模型划分概念域，尤其对视觉模型。**给模型一个概念域的边界清单（建材五金品类概览），比抽象规则有效**——模型看到"镀锌管、密闭套管、八角灯头盒"的具体例子，就知道"4×400"不是品名。

**以后怎么做**：给模型 prompt 时同时给正例和反例 + 概念域清单；关键负向规则（品名不可能只有数字）要在代码层再兜一道。

### 3.2 长提示有效，但"越长越好"是错觉

**发生了什么**：识别 prompt 从 2 行扩到 70 行（品类概览 + 版式示例 + 特征 + 规则），校准 prompt 从 6 条规则扩到 9 条。效果确实提升，但**每加一段都要想清楚它解决哪个具体失败案例**——纯堆字数没有意义。

**学到了什么**：提示词长度服务于"覆盖失败的版式/案例"。每一条规则都应该对应一个真实踩过的坑。示例（few-shot）比抽象描述更有效，因为它直接展示"输入长这样→输出长那样"。

**以后怎么做**：提示词迭代 = 收集失败案例 → 提炼版式/错误类型 → 加示例或规则 → 跑基线验证。提示词是随测试集演化的，不是一次写好的。

### 3.3 同一模型，不同任务要配不同的推理强度

**发生了什么**：校准（机械转换：归一化、拆字段、匹配品名库）经常话痨、输出超限、截断。关闭思考链 + temperature 0.1 + json_object 强制 + 动态 max_tokens 后，秒级完成且稳定。Agent 对话则保留思考链。

**学到了什么**：**校准是转换，对话是推理**。机械转换任务不需要思考，思考反而引入随机性和话痨；推理任务需要思考。任务的"推理需求"决定模型配置，而不是模型统一配置。

**以后怎么做**：给模型分类：纯转换任务（格式化、字段映射、JSON 输出）→ 关思考 + 低温度 + 强制格式 + 严格 token 上限；开放式任务 → 保留思考。每次调用前先问：这个任务需要"想"吗？

---

## 四、关于系统架构：为什么是"多层防线"而不是"一个模型端到端"

### 4.1 多层防线的设计初衷和效果

**发生了什么**：最初设想"识别 → 校准"两步，后来发现每层都不可靠：识别层会输出幽灵行，校准层会乱改。最终架构变成：

```
识别（千问）→ ① 结构化解构（纯代码）
            → ② DeepSeek 语义校准（品名库+拆分+继承）
            → ③ 代码兜底（数值规则+模糊匹配+继承补刀）
```

**学到了什么**：单层模型端到端的错误率是各层错误的累积且不可定位。多层防线的好处：
- **每层职责单一**，出错能定位到层（结构错找解构、语义错找校准、规则错找兜底）
- **确定性层兜底不确定层**——模型的错误在下一层被纠正或标记
- 用户能理解每层在干什么（进度条分步骤显示）

**以后怎么做**：任何"AI 处理真实数据"的链路都按这个模式：**结构层（确定性）→ 语义层（模型）→ 规则层（确定性）**。模型层永远夹在两层代码之间，输出必须经过校验才能进入下一环节。

### 4.2 模型输出要有"硬约束 + 检测 + 降级"三层

**发生了什么**：校准输出经常超过 3800 token 被截断，或输出解释文字混进 JSON。修复：json_object 强制格式、max_tokens 按输入规模动态计算（防话痨也防大单截断）、usage 检测 truncated、**截断时降级为纯规则结果并提示用户**。

**学到了什么**：模型输出不可信是常态，要假设它会截断、会跑题、会乱改。三层防御：**格式硬约束（json_object）→ 截断检测（token 用量）→ 降级路径（模型结果不可信时用确定性结果）**。降级不能静默，要让用户知道。

**以后怎么做**：所有模型调用统一这个模式。动态 max_tokens 的经验公式：`min(上限, 行数×单行预算 + 固定余量)`。

---

## 五、关于决策与协作：用户的工作风格与我踩的坑

### 5.1 "先讨论清楚再动手"是用户的铁律

**发生了什么**：用户多次说"先不要动手，先和我继续讨论""哪里不清楚向我提问，直到你完全清楚再开始行动"。我一次没问就改了 Agent 记忆功能，用户说"我之前并没有设计agent的记忆功能，就只是工具agent而已"——直接回滚。

**学到了什么**：**实现前先确认设计意图**。用户的设计文档（方案/需求）是权威，我在文档之外加的"改进"（比如自作主张加记忆）是越权。用户不是不懂技术，他有自己的架构判断，而且经常比我更了解业务本质。

**以后怎么做**：大改动前先复述需求确认；新增超出需求的能力前先问；被用户否定后立刻回滚，不辩解。

### 5.2 用户讨厌"感觉好了"，要"验证好了"

**发生了什么**：用户说"你不要以为解决了就给我发消息好么，自己测试检验一下，不行就继续找问题继续修继续测试""别吹牛逼了ok？"。他给的方案永远带"验证清单"，要求"每次改动必须有量化结果"。

**学到了什么**：交付 = 实现 + 验证 + 诚实报告。我没验证就宣布解决，比不解决更伤信任。验证要贴近真实场景（真实照片、真实端口、真实调用），不是编译通过就算。

**以后怎么做**：每个改动后至少做一次真实链路验证；报告里区分"已验证"和"未验证"；失败就直说失败，不粉饰。

### 5.3 用户对"太复杂"敏感，对"绕弯路"零容忍

**发生了什么**：历史页日期输入框，我做了 overlay 透明文本、智能光标、格式化占位等一堆自定义控件，全被否定，最后用户说"删除可以顺畅地删完，保留原来的日历选择功能"——回到原生控件。另一次我把端口换来换去，用户说"别老是换来换去的啊"。

**学到了什么**：**能用原生/简单方案解决的就不要自研复杂控件**。自研输入控件的成本（光标、删除、格式化、浏览器兼容）远高于预期，而用户要的可能只是原生控件 + 一个小调整。工程上"简单可靠"优先于"炫酷完整"。

**以后怎么做**：优先原生能力；自定义交互前先想清楚"原生缺什么"；同一个问题反复被否，就停下来问用户到底要什么，而不是换一种复杂方案再试。

### 5.4 环境问题不根治，会反复咬人

**发生了什么**：页面打不开、监控空白、接口 404——根因全是**残留进程抢占端口**（旧 uvicorn 监听 127.0.0.1 拦截新请求、多个 vite 进程）。每次重启都遇到，浪费了大量时间。最后用固定端口 + 杀残留进程根治。

**学到了什么**：开发环境的坑（端口占用、进程残留、缓存、编码）看起来是小问题，但**不根治就会在每个环节反复出现**，消耗的注意力远超技术问题本身。而且用户会因此怀疑整个交付质量。

**以后怎么做**：环境类问题一次定位根因并固化（启动脚本统一清理 + 固定端口）；Windows 下 bash 传中文参数会乱码——中文一律用文件方式/转义，不直接过命令行；区分"控制台显示乱码"和"数据真的坏了"（用文件验证真实内容）。

### 5.5 大文件写入必须验证完整性

**发生了什么**：一次 write 大文件被截断，前端直接 parse error，排查半天。

**学到了什么**：写入大文件后要验证（行数、结尾标记），不能假设成功。前端白屏 = React 渲染崩溃，用"最小化页面 + 逐个恢复"定位崩溃组件。

---

## 六、值得记住的业务本质

- **对账单格式是用户的行业标准**（10列、宋体11pt、合并单元格、公式），AI 系统要迁就它，不是它迁就系统。
- **"已有文件原地修改"和"新建文件放工作目录"是两个不同的操作语义**，用户业务里同时存在，早期只设计了前者。
- 用户对数据的心理预期是"可核对、可修正、可追溯"——所以一切 AI 输出都要可编辑、异常要标红、修正要记录、表头行要标出不删除。**AI 系统不是替代人工核对，是让核对更快更准**。

---

## 结语

这个项目最大的教训可以浓缩成三句话：

1. **确定性问题用代码，语义问题用模型，模型永远夹在两层代码之间。**
2. **配置和权威数据绝不写进提示词，执行层用保险丝强制。**
3. **先讨论清楚再动手，验证好了再交付——用户的设计意图和量化验证高于一切技术偏好。**
