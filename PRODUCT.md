# Product

<!-- impeccable:product-schema 1 -->

## Platform

web（本地优先的单用户工作台；运行端为桌面浏览器。部署硬件假设沿用旧文档：Windows 低配机 + 手机拍照上传——**待重新确认**）

## Users

- 单人使用（确认于 2026-08-08）：建材五金经营者本人，自己操作全部环节
- 非技术背景倾向，术语要用大白话；对金额敏感，怕算错、怕误删
- 实际操作人具备一定命令行能力（当前阶段由产品负责人本人兼开发/验证）

## Product Purpose

把手写送货单从"拍照"变成"可核对、可追溯、可对账"的电子数据：识别出结构化明细 → 人工对照原图确认入库 → 由本地工作助手（agent）完成表格生成与整理等事务。成功 = 账目准确、操作省时、每张单据可溯源。

## Positioning

本地工作台 + 工作助手，不是多角色智能体团队，也不做云端 SaaS。识别（夸克扫描王 image-to-excel）只是背景能力，界面核心是"工作台"：一个页面同时容纳 agent 工作与单据流转（上传→识别→待审核→入库→被 agent 整理进表格）。

## Operating Context

- 每天处理几十张手写送货单，集中在收货时段；单据版式多样（品名单行、名称规格混写、表头混入等）
- 典型场景：正在 agent 页面整理/生成表格时，手边还有一两张新单据，边干活边上传，识别在后台排队，完成后进审核区
- 识别链路（已确认，方案3）：扫描王 image-to-excel → xlsx 内存解析（不落盘、不直接出交付表格）→ 结构化 JSON → 纯代码对齐（品名库/形近字/名称规格拆分/单位补全）→ 人工审核
- 金额不识别：只识别数量/单价，每行金额与合计由代码计算
- 单号与日期自动提取并随结果保留，人工审核可修改，不可丢
- 审核区必须保留原图对照：原图不裁剪，识别结果与上传原图并排对比
- 品名库（约 240 条，含别名与默认单位）是文本对齐的核心资产，保留
- 待审核单据库并入主库：receipts.status（pending/verified/exported），审核区是 pending 的工作视图
- 表格的生成与归属（今天识别的 N 张分到哪张表/哪个 sheet）由 agent 完成

## Capabilities and Constraints

已确认：

- 识别引擎：夸克扫描王 image-to-excel（原 qwen-vl-max 链路弃用）
- 批量：前端一次选 N 张 → 后端并发池（默认 5）→ 统一返回；每张 = 1 次 API 调用（无多图接口）
- 人工审核不可省，但流程弱化：定位是"审核与入库确认"
- 上传入口：位于工作台右侧"单据流"面板，审核区保留上传能力
- DeepSeek 语义校准已删除，校准由纯代码完成；Agent 仍可能需要大模型
- Agent 引擎：**自研**（确认于 2026-08-08），全面吸收 Hermes Agent 的设计取舍——技能文件化（agentskills.io SKILL.md 格式）、三层记忆、会话列表、任务状态、能力边界收窄、后续 cron
- 记忆设计：三层（会话层 / 事实层 / 校正层）。会话=聊天记录+session；事实=对账单路径、默认 sheet、偏好，设置页可见可编辑；校正层=人工修改日志（识别值→修正值），喂给识别对齐，不直接展示
- 技能需要"绑定业务数据"：通用数据选择器（技能元数据声明所需数据类型 → UI 自动生成选择面板：筛选/多选/预览小计），不常驻，随技能参数卡按需展开；右侧单据流是常驻数据入口
- 品名库：独立页面保留，审核区只放一个进入按钮，不内嵌
- 定时功能（如每晚自动整理）：以后做，以 skill 形式存在；本期不做
- Agent 设计不采用"多角色/岗位"模式；定位为单一工作助手

未决：

- 首页具体形态：工作台（中央 agent 工作区 + 右侧单据流/会话面板），以 demo 验证中
- 目标使用者是否纯在线 / 助手未来 skill 数量：影响记忆与模型策略，未最终确认
- 识别产出的展示/交付中间格式（PDF/JSON/xlsx 的取舍）以最近测试结论为准：方案3（xlsx→json）最优

## Brand Commitments

- 产品名 SteelDigitize Pro；中文界面，术语大白话（避免直接暴露 Agent/Token 等词）
- 商务工作台风格：干净、专业、可信，参考飞书/Notion/Teambition 的克制感；不要钢铁工业风
- 状态语义统一：待处理=蓝、已核对=绿、已导出=灰
- 字号偏大（正文 ≥13px、标签 ≥12px），对比度 WCAG AA

## Evidence on Hand

- 五张真实送货单测试图 IMG_6051~IMG_6055（微信原图；backend/uploads/ 有副本）
- 五张识别结果：单号 0001379/0001378/0001377/0000026/0000987，日期 2025-08-31 / 09-02 / 10-03，各 18 行明细
- 项目经验笔记.md：识别分层（结构问题→代码、语义问题→模型/库、规则→兜底）、模型工具描述纪律等
- 前端设计需求说明.md：旧版设计稿（部分已过期——仍写 qwen-vl-max 与 DeepSeek 审核）
- 现有代码：backend/agent.py（自研微 harness）、backend/quark.py（扫描王解析）、backend/calibrate.py（纯代码校准）

## Product Principles

1. 人工审核不可省：原图对照是信任底线，界面必须让人愿意看、看得清
2. 结构问题交给代码，语义问题交给模型/库，规则问题代码兜底（经验笔记 1.1）
3. 金额永远由代码计算，识别只出数量/单价
4. 识别弱化、工作台强化：上传与识别是背景能力，agent 工作与审核是前台
5. 单人使用，界面即工作台；不引入多角色/多账号复杂度

## Accessibility & Inclusion

- 面向中老年经营者视角：字号偏大、对比度达标、步骤少、错误提示明确
- 无其他已确认的无障碍要求
