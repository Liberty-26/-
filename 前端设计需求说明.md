# SteelDigitize — 项目完整介绍（供前端设计参考）

> 用途：本文件用于向设计师 / AI 设计工具完整介绍项目，以便开展前端界面设计。

---

## 一、项目概述

**SteelDigitize（钢单）是一个面向建材五金行业的"手写送货单智能识别与对账系统"。**

店主每天会收到几十张由供应商手写的送货单（白纸黑字、手写品名/规格/数量/单价）。过去，这些单据需要人工逐行录入电脑再对账，费时且容易出错。本系统通过"拍照 → AI 识别 → AI 审核 → 人工核对 → 保存 → Agent 自动对账"的流程，把一张手写单据变成结构化的电子数据，并自动写入对账单（Excel）。

**一句话定位**：让 50 岁的建材老板用手机拍照，就能把一张手写送货单变成一笔清晰的账。

---

## 二、目标用户与使用场景

| 维度 | 说明 |
|---|---|
| 主要用户 | 建材五金店老板 / 经营者，约 45-60 岁，非技术背景 |
| 使用设备 | Windows 电脑（浏览器访问）+ 手机拍照上传 |
| 使用频率 | 每天，处理 20-50 张送货单，集中在下午收货时段 |
| 用户心理 | 要"靠得住、看得清、点得准"的工具；对金额敏感，怕算错、怕误删 |
| 硬件环境 | 低配电脑（双核 CPU / 8GB 内存 / 核显），浏览器 Chrome |

**关键洞察**：
- 用户不关心"技术"，只关心"我的账对不对、快不快"
- 用户的心智模型是"账本 / 单据 / 月份"，不是"数据库 / 记录 / 状态机"
- 字号要够大（50 岁用户），操作要少步骤，错误要有明确提示
- 同一个操作一天重复几十次，快捷键和批量操作是加分项

---

## 三、核心业务流程

```
拍照/上传 → AI识别 → AI审核 → 人工核对 → 保存 → Agent对账
   ①         ②        ③         ④        ⑤       ⑥
```

1. **① 上传**：电脑端拖拽或选择图片，或手机拍照后上传；前端自动压缩图片（宽约 1024px、质量 0.65）
2. **② 识别**：千问视觉大模型（qwen-vl-max）整图理解，提取：单号、日期、明细行（品名/规格/单位/数量/单价）；识别异常自动重试（最多 3 次）
3. **③ AI 审核**：DeepSeek 语义校准 + 品名库匹配 + 代码规则兜底，修正错别字、拆分名称规格混写、标记异常；SSE 流式进度展示（约 8 秒完成）
4. **④ 人工核对**：表格逐格可编辑（键盘 Enter/Tab/Esc 快速操作）、增删行、金额自动重算；红=异常、黄=已修正、灰=疑似表头
5. **⑤ 保存**：单号/日期必填校验，保存后自动归档到"资料库"对应月份账本
6. **⑥ Agent 对账**：勾选多张单据 → 用自然语言下达指令（如"把 7 月的单全部写入对账单"）→ Agent 自动操作 Excel 文件

---

## 四、系统架构

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | React 18 + TypeScript + Vite + Tailwind CSS | 单页应用，5 个功能页，Tab 切换不销毁页面 |
| 后端 | Python FastAPI + SQLite | 单进程部署，同时服务 API 与前端静态文件 |
| 识别模型 | 千问 qwen-vl-max（DashScope） | 视觉大模型，整图理解，temperature 0.1 |
| 审核模型 | DeepSeek v4-flash | 语义校准，JSON 结构化输出 |
| 部署 | http://localhost:8000 单端口 | 低配机友好（内存占用 ~100MB） |

**数据模型（单据）**：单号、日期、明细行数组（品名/规格/单位/数量/单价/金额）、图片、状态（待处理/已核对/已导出）、操作人、总金额。

**品名库**：240 条建材五金品名（含别名、默认单位），是识别校准的核心资产，可手动管理。

---

## 五、功能清单（现状）

### 1. 单据录入页（核心页）
- 图片上传（拖拽/点击，支持 jpg/png/webp）
- 单据日期、单号填写（保存必填，空值红字提示）
- 一键识别（AI 提取明细），识别中显示加载状态
- 一键 AI 审核（SSE 流式三步：文本归一化 → AI 语义校准 → 代码规则兜底）
- 审核摘要：已修正 X 项 / 异常 Y 项 / 疑似表头 Z 行
- 识别结果表格：逐格编辑（键盘支持）、增删行、金额自动计算、异常单元格红黄高亮
- 未入库品名标记（"未入库"橙色徽标，品名不在 240 条库中时）
- 保存入账（校验单号/日期）
- 左侧历史记录：最近 10 张单据快速回载

### 2. 资料库页（历史数据）
- **账本书架视图**：按月归档，每个月是一本"账本"卡片（封面显示年月、单据数、总金额），像书架上的书一样排列（新月份在左上）
- 点击账本 → 进入该月单据列表（面包屑导航"资料库 › 2026年7月"）
- 单据列表：表格（日期/单号/状态/物料概览/金额/操作），支持搜索（书架级=日期+单号+状态；账本级=仅单号）
- 单据详情：展开查看明细行 + 原始照片
- 编辑 / 删除（删除有确认弹窗）
- 未填日期单据归入"未填日期"账本（兜底）

### 3. Agent 对账页
- 单据选择区：按月分组（2026年7月 / 6月…），组内单据以"单号+日期"chip 展示，支持勾选、月份折叠、已选计数
- 对话界面：自然语言指令（"把选中的单据写入对账单"），消息持久化，重启不丢
- 上传已有 Excel 文件（对账单模板，Agent 操作它不移动原位置）
- 技能系统：预设技能快捷指令一键触发；可自定义技能（自然语言描述 → AI 生成技能配置）
- 运行监控面板：总单据/今日处理/已导出/待处理/Token 用量/运行时长（5 秒轮询）

### 4. 品名库页
- 品名 CRUD：新增/编辑/删除
- 字段：品名、别名（可多个）、默认单位
- 用于 AI 审核的归一化匹配（精确 → 别名 → 编辑距离）

### 5. API 与模型设置页
- 视觉模型选择（qwen-vl-plus / qwen-vl-max / qwen3-vl-flash）
- 审核模型选择（DeepSeek）
- API Key 配置、连通性测试（显示模型名与耗时）

---

## 六、当前设计规范（DESIGN.md 要点）

项目已有 Google Stitch 生成的设计规范（"Industrial Logistics"风格），当前代码部分遵循：

- **布局**：固定侧边栏 260px + 流体内容区
- **颜色**：主色蓝 #2563eb / #004ac6，浅灰背景 #f8f9fb，白卡片 + 细边框 #e3e8ef，状态色（绿=成功、琥珀=警告、红=错误）
- **字体**：Inter（中文回退微软雅黑）+ JetBrains Mono（数字/单号等宽）
- **圆角**：6px 基础、8px 卡片、全圆胶囊（状态徽章）
- **组件**：斑马纹表格（表头浅灰、13px）、状态胶囊、步骤条、卡片（白底+细边框+轻阴影）
- **动效**：transition 0.3s cubic-bezier(0.16,1,0.3,1)，hover 颜色整体过渡（过渡色=点击态颜色）

---

## 七、设计目标与约束（重要）

### 设计要解决的核心问题（当前被用户评价"丑、不协调"）
1. **页面没有视觉锚点**：全白浅灰平铺，缺一个"有重量"的区域
2. **流程骨架隐形**：识别/审核/保存三个按钮同权重，用户不知道先点哪个
3. **字号偏小**：标签 10px、表头对比度不足，50 岁用户看不清
4. **账本不像书**：书架卡片是"带蓝条的横卡"，没有账本质感
5. **颜色使用不统一**：多处硬编码色、同一状态两种样式
6. **空状态无引导**：多数页面只有"暂无数据"四个字

### 用户明确要什么
- **商务工作台风格**（不要钢铁/工业硬核风，不要花哨）：干净、专业、可信
- 结构：顶部栏（品牌+操作）+ 左侧导航 + 内容区卡片化
- 中文界面，术语要"大白话"（避免"Agent/Token/归一化"这类词直接暴露）
- 字号偏大（≥13px 正文，标签 ≥12px），对比度达标（WCAG AA）
- 状态色语义统一（待处理=蓝、已核对=绿、已导出=灰）
- 交互：hover 颜色整体平滑过渡（0.3s cubic-bezier(0.16,1,0.3,1)），点击态=过渡目标色

### 约束
- 功能不能少（五页功能全部保留），设计只改"皮"不改"功能逻辑"
- 前端与后端接口契约不变（API 字段不动）
- 低配机运行，动效要轻量，不引入重型动画库
- 参考风格：飞书工作台、Notion、Teambition 等现代商务 SaaS 的克制感

---

## 八、给设计方的建议交付物

1. 完整工作台布局（顶栏 + 侧边栏 + 内容区骨架）
2. 五个页面的视觉稿：工作台首页 / 单据录入（核心）/ 资料库（书架）/ Agent / 品名库 / 设置
3. 设计 token（颜色/字体/圆角/间距/阴影）落地为 Tailwind 配置
4. 关键组件样式：状态胶囊、步骤条、账本卡片、表格、空状态、弹窗
5. 优先保证：单据录入页（主流程）与资料库书架（视觉记忆点）
