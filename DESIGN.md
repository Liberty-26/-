# 数字化工作台 — 设计系统

> 记录：2026-08-09 · 视觉方向确立（v1.0.8 起生效）

## 方向契约

- **THESIS**：这是一个"账本工作台"而不是网页后台——深色导航栏把操作区与内容区分开，浅灰画布上白色纸面卡片承载业务内容，让待审核/识别中/已完成三种状态一眼可辨。
- **OWN-WORLD**：墨蓝侧栏（`#152238`）+ 浅灰画布（`#f4f5f8`）+ 白色纸面卡片（`#ffffff`）；主操作蓝 `#2563eb`、待审核琥珀 `#d97706`、完成绿 `#16a34a`、错误红 `#dc2626`；卡片细边框 + 轻阴影，圆角 14px。
- **STORY**：使用者打开就是自己的工作台：左边是固定的导航与全局待审角标，中间是当天的单据流转，右侧是单据流/会话；识别结果与上传原图并排对照，人工审核是信任底线。
- **FIRST VIEWPORT**：深色侧栏（品牌"数字化工作台"）→ 工作台中央（技能卡片 + 助手对话）→ 右侧单据流；审核区保持"左队列 + 右原图/结果"双栏，原图与结果各占约一半横向空间。
- **FORM**：Operate 模式；克制增量重设计，不引入概念大赛；所有状态色沿用既有语义（蓝=处理中、琥珀=待审核、绿=已核对、红=错误）。

## 令牌

侧边栏：`--nav-bg:#152238`、`--nav-text:#c8d4e3`、`--nav-text-dim:#8194ad`、`--nav-hover:rgba(255,255,255,.08)`、`--nav-border:rgba(255,255,255,.09)`。

内容区沿用：`--bg:#f4f5f8`、`--card:#fff`、`--border:#e4e7ec`、`--primary:#2563eb`、`--warn:#d97706`、`--ok:#16a34a`、`--err:#dc2626`。

## 组件约定

- 导航：深色底、浅色文字；激活项 = 品牌蓝实底白字；角标半透明白底。
- 卡片：白底、1px 边框、轻阴影；不做双层卡片。
- 删除操作：统一"禁止符号"图标（圆圈 + 横线），平时淡灰、hover 变红。
- 审核区：原图/结果各约 48%/52%，宽屏原图上限 760px；原图滚轮缩放不触发页面滚动。
- 语义色只做状态与主操作，不做装饰渐变。
