# 数字化工作台 — 设计系统

> 记录：2026-08-09 · 视觉方向：现代简约 Minimalism（vibe-hub.org/style-minimal）

## 方向契约

- **THESIS**：这是一个"操作型效率工具"而不是装饰性后台——近黑正文、1px 细分割线、留白与字重层级组织页面，强调色只做主操作/链接/关键状态，让表格与审核成为绝对主角。拒绝深色侧栏、大色块状态、装饰性阴影与渐变。
- **OWN-WORLD**：浅灰画布（`#f7f8f9`）+ 白色表面（`#ffffff`）+ 1px 细线（`#e6e8ea`）；近黑正文（`#0a0a0c`）、次要灰（`#6d6d76`）、弱灰（`#9aa1aa`）；主操作蓝（`#2563eb`）、待审核琥珀（`#b45309`）、已核对绿（`#15803d`）、错误红（`#dc2626`）；圆角 8–10px；阴影只给浮层（弹窗/抽屉）。
- **STORY**：打开就是自己的工作台：左侧极简导航，顶部/审核区一条"AI 已完成什么、等你核对什么"的状态条，中间是对账表格与单据流；审核区原图与识别结果始终左右同屏，不需要上下滚动。
- **FIRST VIEWPORT**：白色侧栏（品牌点 + "数字化工作台"）→ 工作台中央（AI 状态条 + 技能 + 助手对话）→ 右侧单据流；审核区"左队列 + 右原图/结果分屏"。
- **FORM**：Operate 模式；现代简约；一切层级靠排版与分组，不靠装饰。

## 令牌

- 底色：`--bg:#f7f8f9`、`--card:#ffffff`、`--border:#e6e8ea`、`--border-strong:#d9dce0`
- 文字：`--text:#0a0a0c`、`--text-2:#6d6d76`、`--text-3:#9aa1aa`
- 强调：`--primary:#2563eb`、`--primary-strong:#1d4fd7`、`--primary-soft:#f1f5fd`
- 状态：`--ok:#15803d`、`--ok-soft:#f0faf3`、`--warn:#b45309`、`--warn-soft:#fdf8f0`、`--err:#dc2626`、`--err-soft:#fdecec`
- 阴影：普通内容无阴影；浮层（弹窗、全屏图、Toast）可用深色大阴影
- 数字：金额一律等宽数字（`tabular-nums`），合计由代码计算

## 组件约定

- 导航：白底 + 1px 右边框；激活项 = 浅蓝底 + 蓝字，不用实底大色块
- 卡片/表格：白底 + 1px 边框，无阴影；表头浅灰底、细分割线
- 状态：圆点/细字或柔和浅底徽标，不做高饱和大色块
- 审核区：原图（左）与识别结果（右）分屏同框，各自内部滚动，页面本身不滚动
- AI 可见性：工作台与审核区顶部各有一条"AI 状态条"（真实计数），审核结果表保留修正/异常标注
- 弹窗：半径 12px + 深色阴影（浮层层级），背景毛玻璃
